import java.util.concurrent.TimeUnit;

public class DataStreamJob {

    private static final Logger logger = LoggerFactory.getLogger(DataStreamJob.class);

    public static void main(String[] args) throws Exception {

        long startTime = System.nanoTime(); // Record the start time

        // Your existing code

        long endTime = System.nanoTime(); // Record the end time

        long duration = TimeUnit.NANOSECONDS.toMillis(endTime - startTime); // Calculate the duration in milliseconds

        logger.info("Loading time: " + duration + " ms");
    }
}
